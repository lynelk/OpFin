include: package:flutter_lints/flutter.yaml

# Flutter 3.47.1's standard migration, recorded so a release build stays clean.
# Application and test Dart sources remain covered by analysis.
analyzer:
  exclude:
    - build/**
    - android/**
    - ios/**
    - web/**
    - windows/**
    - macos/**
    - linux/**
