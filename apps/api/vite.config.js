import { defineConfig } from 'vite';
import laravel from 'laravel-vite-plugin';

export default defineConfig({
    css: {
        preprocessorOptions: {
            scss: {
                // Our SCSS uses the module system. Keep upstream Bootstrap deprecations
                // from drowning release logs until Bootstrap removes its legacy internals.
                quietDeps: true,
            },
        },
    },
    plugins: [
        laravel({
            input: [
                'resources/sass/app.scss',
                'resources/js/app.js',
            ],
            refresh: true,
        }),
    ],
});
